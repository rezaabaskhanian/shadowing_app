<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- ضبط صدای کاربر در مرحله‌ی Record (react-native-nitro-sound) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- بدون این، اندروید ۱۳+ درخواست مجوز نوتیفیکیشن (Firebase messaging
         requestPermission) رو بی‌صدا رد می‌کنه و توکن FCM هیچ‌وقت ثبت نمی‌شه. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- پرمیژن‌های پخش پس‌زمینه‌ی react-native-track-player را خودِ کتابخانه
         با manifest merge اضافه می‌کند؛ اینجا لازم نیست تکرار شود. -->

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>
    </application>
</manifest>
